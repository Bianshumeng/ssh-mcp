// Added for testing purposes, requires docker compose to run
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "ssh-mcp-server": {
      "type": "local",
      "command": [
        "node",
        "build/index.js",
        "--config=test/fixtures/profiles.local.yaml",
        "--profile=no-pass",
        "--timeout=60000"
      ],
      "enabled": true,
    },
  },
}
