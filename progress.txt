已完成:
- 拉取上游 ssh-mcp 仓库并完成现状审计。
- 建立 plan 文档体系与本任务提案/清单/技术设计/规格变更。
- 完成配置层、profile 生命周期层、SSH 连接层、工具层拆分重构。
- 新增 profile 管理工具：profiles-list/use/reload/note-update。
- 新增示例配置与 README 新章节（配置格式/兼容模式/新工具说明）。
- 完成新增测试并通过：配置解析、冲突参数、备注持久化、目标切换校验。
- 已提交并推送到个人仓库：`https://github.com/Bianshumeng/ssh-mcp.git`（commit: `64bb3ac`）。
- 已完成敏感信息审计：移除示例中的真实 IP 与本机路径，改为通用占位。
- 按用户要求下线 legacy 启动模式：`--host/--user/...` 不再支持，`--config` 成为唯一入口。
- 同步更新文档与测试夹具，集成测试默认走 `test/fixtures/profiles.local.yaml` 配置模式。
- 完成安全修复：配置写入改为“先验证后持久化”，修复 createProfile 失败脏写问题。
- 完成安全修复：备份路径 reason 做文件名清洗 + 目录逃逸保护，修复删除准备阶段路径风险。
- 完成安全修复：备份文件写入权限固定为 owner-only（POSIX 0600）。
- 完成安全修复：删除无关 profile 时，持久化 activeProfile 不再受 `--profile` 启动覆盖干扰。
- npm 包已发布到 `@xiaoyankonling/ssh-mcp@2.0.0`。

进行中:
- 等待用户验收与确认是否统一提交“仅配置模式 + 安全修复”改动。
- 等待用户确认是否提交并推送“仅配置模式 + 安全修复 + review 回归修复”整包改动。

下一步:
- 根据验收结果决定是否执行归档步骤（plan/归档）。
- 确认是否提交并推送本次包名/README/锁文件变更。

已知问题:
- `npm test` 全量中含大量 live SSH 集成用例，当前环境若未启动 127.0.0.1:2222 的测试 SSH 服务会失败。
